rootProject.name = 'rim-tool'

includeBuild('hirs') {
    dependencySubstitution {
        substitute(module('org.hirs:HIRS_Utils')).using(project(':HIRS_Utils'))
    }
}

dependencyResolutionManagement {
    versionCatalogs {
        create("libs") {
            from(files("gradle/versions.toml"))
        }
    }
}